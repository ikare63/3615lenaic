# 3615 NEXUS — miroirs JSON

Ce dossier est alimenté automatiquement par GitHub Actions environ toutes les 3 heures.
Les données privées du navigateur ne sont jamais écrites ici : elles restent dans le coffre local NEXUS (IndexedDB) jusqu'à un export manuel.
